# Lumina Finance — npm configuration
# React 19 ecosystem transition: several mature libraries (recharts, etc.) still
# declare React 18 peer ranges while being runtime-compatible with React 19.
# legacy-peer-deps keeps installs deterministic during this transition window.
legacy-peer-deps=true
save-exact=false
fund=false
audit=false
